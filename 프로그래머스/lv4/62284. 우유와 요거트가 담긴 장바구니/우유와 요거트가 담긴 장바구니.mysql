-- 코드를 입력하세요

select distinct(cart_id) 
from CART_PRODUCTS
where cart_id in 
(select distinct(cart_id) from CART_PRODUCTS where name = 'Milk')
and cart_id in (select distinct(cart_id) from CART_PRODUCTS
where name = 'Yogurt')

